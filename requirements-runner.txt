# =============================================================================
# SeleniumBase CI Runner 基础依赖
# 这个文件被 Dockerfile 使用，打包进镜像
# 测试仓库自身的 requirements.txt 在运行时由 entrypoint.sh 额外安装
# =============================================================================

# ── 测试框架 ──────────────────────────────────────────────────────────────────
pytest>=7.4.0,<9.9
pytest-json-report>=1.5.0   # 生成 test-report.json，平台解析结果依赖
pytest-metadata>=3.0.0      # pytest-json-report 的依赖
pytest-html>=4.0.0          # HTML 报告（可选，便于人工查看）

# ── SeleniumBase（含 selenium、webdriver-manager 等完整依赖）─────────────────
seleniumbase>=4.30.0

# ── 数据库 / HTTP（测试用例中常用）──────────────────────────────────────────
pymysql>=1.1.0
requests>=2.31.0
